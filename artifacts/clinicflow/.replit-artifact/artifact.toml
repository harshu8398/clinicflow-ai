kind = "web"
previewPath = "/"
title = "ClinicFlow AI"
version = "1.0.0"
id = "artifacts/clinicflow"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25283

[services.development]
run = "pnpm --filter @workspace/clinicflow run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/clinicflow", "run", "build" ]
publicDir = "artifacts/clinicflow/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25283"
BASE_PATH = "/"
